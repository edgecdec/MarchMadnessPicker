# Bugs — Fix these BEFORE working on any PLAN.md tasks

- **CRITICAL - First Four game results are being counted in the score**: The bracket page shows "🏆 Score: 0 / 2 possible" after 2 First Four games finished. This is WRONG — First Four games are play-in games that users do NOT pick. The score should show "0 / 0 possible" until R64 games start being resolved. The scoring logic and/or the score display is including First Four results in the "possible" count. Fix: identify how First Four results are stored in results_data (what game IDs do they use?) and exclude them from BOTH the scorePicks() function and the maxPossibleScore() / score display on the bracket page. Only game IDs matching the 63 pickable games (R64 through Championship) should count. DO NOT mark this as fixed without verifying the bracket page shows "0 / 0 possible" when only First Four games have been played.
