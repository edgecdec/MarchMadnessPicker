# Bugs — Fix these BEFORE working on any PLAN.md tasks








- **Matchup top border should be region color, not white**: Each matchup box has a white top border that looks out of place. Change it to use the region's accent color (East=blue, West=red, South=green, Midwest=orange) so the matchups visually belong to their region. Apply to all matchup boxes in all rounds within each region.
