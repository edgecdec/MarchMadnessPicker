# Bugs — Fix these BEFORE working on any PLAN.md tasks



- **Highlight the current user's brackets on the leaderboard**: On the leaderboard page, the logged-in user's own bracket rows should be visually highlighted (e.g. subtle background color or left border accent) so they can quickly find themselves among many entries.
