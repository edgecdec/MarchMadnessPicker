# Bugs — Fix these BEFORE working on any PLAN.md tasks



- **Adjust medium bracket layout breakpoints**: The medium/tablet bracket layout currently kicks in at a certain max width but it should be used for a wider range — from its current minimum up to 1.5x its current max pixel breakpoint. For example if it currently shows between 768-1200px, change it to 768-1800px. This way more users on smaller desktop windows or tablets get the medium layout instead of being forced into the full desktop view that doesn't fit their screen.
