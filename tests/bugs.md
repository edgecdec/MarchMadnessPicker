# Bugs — Fix these BEFORE working on any PLAN.md tasks

- **CRITICAL - Max possible score only counts R64 (shows 704 instead of 2541)**: The max possible score on the leaderboard shows 704 for everyone, which is exactly 22 × 32 (R64 points only). The real max with the Everyone group scoring [22,33,55,88,143,231] should be 2541 (704+528+440+352+286+231). The maxPossibleScore function is likely only counting games that have results in results_data, but since no R64+ games have been played yet, it should be calculating based on ALL 63 games being potentially correct, not just resolved ones. Also, people with incomplete brackets should NOT show the same max as people with complete brackets — their max should be lower since they have fewer picks that could be correct. Fix: max possible = sum of points for all games where the user HAS a pick AND the picked team is still alive (not eliminated). For games not yet resolved, assume the pick could be correct.

- **Leaderboard status column shows "checked" for incomplete brackets**: The status column should only show ✅ if the bracket has all 63 picks filled. Brackets with fewer than 63 valid picks (excluding ff-play-* junk keys) should show ⏳ or the number of missing picks. The check is likely counting total keys including First Four junk keys, inflating the count.
