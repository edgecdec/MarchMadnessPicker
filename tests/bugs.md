# Bugs — Fix these BEFORE working on any PLAN.md tasks










- **Mobile bracket tabs should show 2 rounds side by side, not 1 round**: The mobile bracket currently shows one round per tab as a flat list. Instead, each tab should show two rounds side by side like a mini bracket: Tab 1 = R64 (left) → R32 (right), Tab 2 = Sweet 16 (left) → Elite 8 (right), Tab 3 = Final Four & Championship (already fine). Within each tab, the R64 games on the left should visually feed into the R32 games on the right, with the R32 game vertically centered between its two R64 feeder games. Same for S16→E8. This gives the user the bracket feel on mobile instead of just a list of games.
