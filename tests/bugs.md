# Bugs — Fix these BEFORE working on any PLAN.md tasks

- **CRITICAL - First Four winners not promoted into bracket_data, breaking live scores and sync**: The regions array still has the First Four placeholder team (NC State, espnId 152) instead of the actual winner (Texas, espnId 251). This breaks live score matching and ESPN sync for any game involving a First Four winner. FIX: In espnSync.ts, after syncing First Four results, automatically update bracket_data.regions — for each resolved First Four game, find the matching seed slot in the regions array and replace the name and espnId with the winner's. The first_four array already has both teams' names and espnIds. Determine the winner from results_data, then swap it into regions. Save the updated bracket_data to the DB. This makes First Four feed into R64 just like any other round feeds into the next — the winner's info flows forward automatically. Do this for all 4 First Four slots. After updating, trigger another sync pass so R32+ games involving First Four winners get matched correctly.
