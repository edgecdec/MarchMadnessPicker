# Bugs — Fix these BEFORE working on any PLAN.md tasks


- **Hide tiebreaker scores on leaderboard until brackets lock**: The leaderboard currently shows everyone's tiebreaker prediction (championship game total score) before lock time. This lets users copy each other's tiebreaker values. Hide the tiebreaker column/values on the leaderboard until after the tournament lock_time has passed. Before lock, show "Hidden" or just omit the column entirely.

- **CRITICAL - Region placement and Final Four pairings are WRONG**: The correct 2026 overall seed order is Duke #1, Arizona #2, Michigan #3, Florida #4. Layout: Top-left=EAST (Duke #1), Top-right=WEST (Arizona #2), Bottom-right=MIDWEST (Michigan #3), Bottom-left=SOUTH (Florida #4). Semis are LEFT vs LEFT and RIGHT vs RIGHT: Semifinal 1 = East (top-left) vs South (bottom-left), Semifinal 2 = West (top-right) vs Midwest (bottom-right). So ff-4-0 = East vs South, ff-4-1 = West vs Midwest. The regions array in bracket_data must be ordered [East, West, South, Midwest] so the layout renders: top=[East, West], bottom=[South, Midwest]. Update FinalFour.tsx: ff-4-0 = regions[0] (East) vs regions[2] (South), ff-4-1 = regions[1] (West) vs regions[3] (Midwest). Also update bracket_data on the server to reorder regions to [East, West, South, Midwest]. NCAA rule: #1 top-left, #2 top-right (opposite side), #3 bottom-right (same side as #2), #4 bottom-left (same side as #1). This ensures #1 and #2 can only meet in the championship.
