# Bugs — Fix these BEFORE working on any PLAN.md tasks

- **Leaderboard: player name column too narrow, number columns too wide**: The player/bracket name column needs to be about 3x wider than it currently is. The numeric columns (score, round scores, max possible, etc.) are taking up too much space — they only need to fit small numbers, so reduce their width. This should make player names readable without truncation while keeping everything on screen.
